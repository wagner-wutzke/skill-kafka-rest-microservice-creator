# skill-kafka-rest-microservice-creator

A Codex Skill for generating base Spring Boot Microservice projects with REST endpoints and Kafka Producer / Consumers.
Source code and configurations are generated based on a predefined domain objects, like `product`, `customer`, or `order`.
The domain object is to be defined as an AVRO spec file.
